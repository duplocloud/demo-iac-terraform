# EFS Module - Terraform configuration
# This file contains the Terraform and provider configuration blocks

terraform {
  required_version = ">= 1.5.0"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = ">= 5.0"
    }
  }
}
